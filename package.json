{
  "name": "falah-studios",
  "version": "1.0.0",
  "description": "Falah Studios - Creative Technology Studio - Full Stack Application",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "apps/web",
    "apps/api",
    "apps/os"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace @falah/web dev\" \"yarn workspace @falah/api dev\"",
    "build": "yarn workspace @falah/web build && yarn workspace @falah/api build",
    "start": "yarn workspace @falah/api start",
    "web:dev": "yarn workspace @falah/web dev",
    "web:build": "yarn workspace @falah/web build",
    "api:dev": "yarn workspace @falah/api dev",
    "api:build": "yarn workspace @falah/api build",
    "lint": "yarn workspace @falah/web lint && yarn workspace @falah/api lint",
    "type-check": "yarn workspace @falah/web type-check && yarn workspace @falah/api type-check"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/morgan": "^1.9.10",
    "concurrently": "^8.2.2"
  }
}
