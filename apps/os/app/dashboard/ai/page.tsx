"use client";
import { useState } from "react";
import { api } from "../../../lib/api";
export default function AiPage() { const [answer, setAnswer] = useState(""); const [loading, setLoading] = useState(false); async function ask() { setLoading(true); try { setAnswer((await api<{ response: string }>("/business/ai/insights", { method: "POST" })).response); } finally { setLoading(false); } } return <main className="min-h-screen bg-black text-white"><header className="border-b border-zinc-900 px-6 py-6 lg:px-10"><p className="text-[10px] tracking-[.3em] text-zinc-600 uppercase">Business assistant</p><h1 className="mt-1 text-xl font-medium">Falah AI</h1></header><div className="max-w-3xl px-6 py-10 lg:px-10"><section className="rounded-2xl border border-zinc-900 bg-zinc-950 p-7"><h2 className="text-2xl font-semibold tracking-tight">How is my business doing?</h2><p className="mt-3 text-sm leading-6 text-zinc-500">Get a private overview based only on your workspace’s orders, customers, and bookings.</p><button onClick={ask} disabled={loading} className="mt-7 rounded-xl bg-white px-5 py-3 text-xs font-medium text-black">{loading ? "Reviewing..." : "Generate business insight"}</button>{answer && <div className="mt-7 rounded-xl border border-zinc-800 bg-black p-5 text-sm leading-6 text-zinc-300">{answer}</div>}</section></div></main>; }
